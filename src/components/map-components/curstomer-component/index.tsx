/**
 * 此组件以基于zrender画室内示意图
 * 为了简化流程，这里以简单的方式来
 * 共分为三层
 * 1. 最底层为示意地图层，主要由客户提供，预计是 cad 或 svg
 * 2. 中间层为轨迹层，由定位数据来决定
 * 3. 最上层为交互层，主要用来画围栏
 * @export
 * @return {*}
 */
export function ZrenderComponent() {
  return <h1></h1>;
}
